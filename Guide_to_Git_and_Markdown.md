# A-Beginner-s-Guide-to-GIt-and-Markdown

## Содержание
___
* [Введение](#введение)    

* [Markdown](#marckdown)    
    * [Заголовки](#заголовки)
    * [Начертание](#Начертание)
    * [Списки](#списки)
    * [Ссылки](#Ссылки)
    * [Изображения](#изображения)
    * [Выделение элементов](#выделениеэлементов)
    * [Таблицы](#таблицы)
    * [Цитаты](#цитаты)
    * [Линия](#линия)

* [Git](#git)
    * [Авторизация](#авторизация)
    * [Инициализация репозитория](#инициализация-репозитория)
    * [Добавление файлов в область подготовленных файлов](#добавление-файлов-в-область-подготовленных-файлов)
    * [Внесение изменений](#внесение-изменений)
    * [История коммитов](#история-коммитов)
    * [Просмотр изменений до коммита](#просмотр-изменений-до-коммита)
    * [Переименование файлов](#переименование-файлов)
    * [Перемещение по проекту](#перемещение-по-проекту)
    * [Создание новой ветки и переход в неё](#создание-новой-ветки-и-переход-в-неё)
    * [Просмотр списка веток](#просмотр-списка-веток)
    * [Удаление ветки](#удаление-ветки)
    * [Слияние веток](#слияние-веток)
    * [Отображение журнала фиксации в виде графика для текущей или всех веток](#отображение-журнала-фиксации-в-виде-графика-для-текущей-или-всех-веток)
    * [Отправка изменений в удалённый репозиторий](#отправка-изменений-в-удалённый-репозиторий)
    * [Получение изменений из удалённого репозитория](#получение-изменений-из-удалённого-репозитория)
    * [Слияние удалённого репозитория с локальным](#слияние-удалённого-репозитория-с-локальным)

# [Введение](#содержание)

**Git** - самая популярная в мире распределенная система контроля версий. **Линус Торвальдс**, разработчик ядра OC Linux, создал этот инструмент еще в далеком 2005 году, а сегодня Git поддерживается как преокт с активным исходным кодом.

**Markdown** — это облегченный язык разметки с синтаксисом форматирования обычного текста. Первоначально создан в 2004 году **Джоном Грубером** и **Аароном Шварцем**. Многие идеи языка были позаимствованы из существующих соглашений по разметке текста в электронных письмах. 

# [Marckdown](#содержание)

Ниже будут приведены примеры использования Markdown, однако это далеко не полное руководство. Символы, которые обычно рассматриваются в Markdown как специальные, могут быть экранированы с помощью обратного слеша. Например, последовательность «\*» выведет символ «*», а не будет являться признаком начала выделенного текста. Кроме того, Markdown не преобразует текст внутри «сырых» блоков XHTML. Таким образом, в Markdown-документ можно включать секции XHTML, заключив их предварительно в теги уровня блока.

## [Заголовки](#содержание)
___

Рассмотрим написание заголовков с примером: 

\# Заголовок 1

\#\# Заголовок2

\#\#\# Заголовок 3 

\#\#\#\# Заголовок 4

\#\#\#\#\# Заголовок 5

\#\#\#\#\#\# Заголовок 6

Результатом станет следующее:
# Заголовок 1
## Заголовок 2
### Заголовок 3
#### Заголовок 4
##### Заголовок 5
###### Заголовок 6

## [Начертание](#содержание)
___

Выделение участков текста - неотъемлимая часть любого текстового редактора. **Marckdown** не стал исключением и пришло время узнать как именно это делается:

* _Курсив_

Курсив обозначается одинарными \*звездочками\* или \_подчеркиванием\_

* **Полужирный**

Полужирный текст обозначается в языке Markdown двойными \*\*звездочками\*\* или двойным \_\_подчеркиванием \_\_

* ~~Зачеркнутый~~ Перечеркнутый текст

Две \~\~ в начале и в конце перечеркивают текст: ~~текст не ограничается одним словом~~. 

* Хочется отметить, что альтернатива курсивному или полужирному выделению текста была создана не просто так. Их можно комбинировать:
    
    * Пример 1: \_\_\*Пример 1\*\_\_ - __*Пример 1*__
    * Прмиер2: \*\*\_Пример 2\_\*\* - **_Пример 2_**

## [Списки](#содержание)
___

* **Нумерованные списки**

Создаются посредставм указания числовых значений в начале строки, например написав следующий список -

\1. Первый элемент списка

\3. Второй элемент списка

\6. Третий элемент списка 

\23. Четвертый элемент списка

\- мы получим следующий результат:

1. Первый элемент списка
3. Второй элемент списка
6. Третий элемент списка 
23. Четвертый элемент списка

Языку Markdown абсолютно неважно какое числовое значение будет указано вначале строки, главное что отсчет начнется с первого указанного числа. 

* **Ненумерованные списки**

Списки могут быть выделены следующими способами:

\* Первый элемент списка 

\- второй элемент списка

\+ третий элемент списка

В результате мы имеем:

* Первый элемент списка
- Второй элемент списка
+ Третий элемент списка
    * подсписок 1
    * подсписок 2
    
    Так же текст можно выравнивать по элементу списка.

## [Ссылки](#содержание)
___

**Есть 3 способа создать ссылку**:
1. Создание с указанием ссылки сразу
    * [С указанием ссылки сразу] (https://github.com/v1rasta/A-Beginner-s-Guide-to-GIt-and-Markdown/blob/main/Guide_to_Git_and_Markdown.md#содержание)
2. С указанием ссылки в сноске
    * [С указанием ссылки в сноске] [Ссылка]
    * [Можно использовать цифры в качестве сносок] [1]
3. Так же любая ссылка, обрамленная в угловые скобки, автоматически преобразуется в ссылку \*\*<\*\*https://github.com/v1rasta/A-Beginner-s-Guide-to-GIt-and-Markdown/blob/main/Guide_to_Git_and_Markdown.md#\*\*>\*\*

**На практике мы получаем следующее**:

[Создание ссылки сразу](https://github.com/v1rasta/A-Beginner-s-Guide-to-GIt-and-Markdown/blob/main/Guide_to_Git_and_Markdown.md#содержание)

С указанием ссылки в сноске [1](https://github.com/v1rasta/A-Beginner-s-Guide-to-GIt-and-Markdown/blob/main/Guide_to_Git_and_Markdown.md#содержание)

<https://github.com/v1rasta/A-Beginner-s-Guide-to-GIt-and-Markdown/blob/main/Guide_to_Git_and_Markdown.md#содержание>

## [Изображения](#содержание)
___

**Есть 2 способа задать изображение:**
1. Ссылка на изображение сразу
    * \![текст]\(ссылка)
2. Ссылка на изображение в сноске
    * \![текст]\[картинка]
    * \[картинка]: адрес

В случае если картинка не может подгрузиться - вместо нее указывается [текст]

**Рассмотрим на примере:**
1. ![Здесь может быть ваша реклама](https://yandex.ru/images/search?pos=14&text=прикольные%20картинки%20про%20программистов%5C&img_url=http%3A%2F%2Fbipbap.ru%2Fwp-content%2Fuploads%2F2017%2F01%2F224024_a48.jpeg&rpt=simage&lr=51)
2. ![Здесь может быть ваша реклама][Принт]

[Принт]: https://yandex.ru/images/search?pos=2&img_url=http%3A%2F%2Fbipbap.ru%2Fwp-content%2Fuploads%2F2017%2F01%2Fc3946164efffffbb503b73b16a75ccb3.jpg&text=прикольные%20картинки%20программистов&lr=51&rpt=simage&source=serp

## [Выделение элементов](#содержание)
___

Текст может быть обрамлен с помощью апострофов( \` ), например `он будет выглядеть так`. Большие куски кода обрамляются 3+ апострофами, а при добавлении префикса языка програмирования подсветка меняется.

## [Таблицы](#содержание)
___

Таблицы в **markdown** Обязательно требуют заголовок и настройки выравнивания (второй строкой). Выравнивание задаётся двоеточием. Колонки задаются вертикальным палками (|). Начиная с третьей строки соблюдение 

Пример:

|1 столбец  |2 столбец  |3 столбец  |4 столбец  |
|----------:|:---------:|:---------:|:---------:|
*3 строка*|`3 строка` |**3 строка**|_**3 строка**_

Начиная с 3 строки нет необходимости подгонять колонки под один размер как и задавать внешние вертикальные палки.

*Выглядит выше указанная таблица так*:

|1 столбец  |2 столбец  |3 столбец  |4 столбец  |

|----------:|:---------:|:---------:|:---------:|

\*3 строка\*|\`3 строка`|\*\*3 строка**|\_\*\*3 строка**_

## [Цитаты](#содержание)
___

Цитаты задаются угловой скобкой > в начале строки. Объем текста при этом не ограничивается. Приведем пример:

> Аккуратный программист — быстрый программист

© Билл Гейтс.

Рассмотрим еще один пример написания цитат:

\>Данная строка является частью цитаты.\

\>Впрочем как и эта строка.

В данной строке мы прерываем цитату.

Как это выглядит на практике:
>Данная строка является частью цитаты.\
>Впрочем как и эта строка.

В данной строке мы прерываем цитату.

## [Линия](#содержание)
___

Задать линию можно благодоря написанию трех или более символов:

\-\-\- 

Дефис

\*\*\*

Звездочки

\_\_\_

Подчеркивания

Результат:
___
Дефис
***
Звездочки
___
Подчеркивания

# [Git](#содержание)

В данной статье перечисляются самые основные команды, которые следует знать разработчику, чтобы освоить управление репозиториями **GitHub** на высоком уровне. Ознакомиться с ними будет полезно как новичкам, так и опытным разработчикам.

## [Авторизация](#содержание)
___

Имя пользователя нужно, чтобы привязывать коммиты к вашему имени. Задать или изменить имя пользователя можно с помощью команды *git config*. Новое имя будет автоматически отображаться в последующих коммитах, отправленных на **GitHub** через командную строку.\
Кроме того, командой *git config* можно изменять адрес электронной почты, привязанный к вашим коммитам **Git**. Новый адрес электронной почты так же будет автоматически отображаться во всех дальнейших коммитах.\
В терминале команды будут выглядеть следующим образом:

    git config --global user.name "Михаил v1rasta Копкин"
    git config --global user.email "v1rasta163@gmail.com"

## [Инициализация репозитория](Содержание)
___

Создать пустой репозиторий **Git** или вновь инициализировать существующий можно параметром *init*. При инициализации он создаст скрытую папку. В ней содержатся все объекты и ссылки, которые **Git** использует и создаёт в истории работы над проектом.

    git init

## [Добавление файлов в область подготовленных файлов](#содержание)
___

Добавить отдельный файл в область подготовленных файлов можно параметром *add* с указанием имени файла. Что бы не прописывать имя файла вручную можно добиться необходимого результата прибегнув к табуляции.

    git add .\name_your_file

## [Проверка статуса репозитория](#содержание)
___

Просмотреть статус нужного репозитория можно по ключевому слову *status*: его действие распространяется на подготовленные, неподготовленные и неотслеживаемые файлы.


    git status

## [Внесение изменений](#содержание)
___

При создании коммита в репозитории можно добавить однострочное сообщение с помощью параметра *commit* с флагом *-m*. Само сообщение вводится непосредственно после флага, в кавычках. 

    git commit -m "Name_your_commit"

## [История коммитов](#содержание)
___

Просматривать изменения, внесённые в репозиторий, можно с помощью параметра *log*. Он отображает список последних коммитов в порядке выполнения.

    git log

## [Просмотр изменений до коммита](#содержание)
___

Можно просматривать список изменений, внесённых в репозиторий, используя параметр *diff*. По умолчанию отображаются только изменения, не подготовленные для фиксации.

    git diff

Для просмотра подготовленных изменений необходимо добавить флаг *--staged*

    git diff --staged

Также можно указать имя файла как параметр и просмотреть изменения, внесённые только в этот файл

    git diff namefile.md

## [Переименование файлов](#содержание)
___

Переименовать файл или папку можно параметром *mv*. Для него указывается источник *source* и назначение *destination*. Источник — реально существующий файл или папка, а назначение — существующая папка.

    git mv dir1/namefile.md dir2

## [Перемещение по проекту](#содержание)
___

Для перемещения по проекту между комитами или ветками можно воспользоваться параметром *checkout*, используя хеш коммита или имя ветки.

    git checkout commit_hash
    git checkout branch_name

## [Изменение последнего коммита](#содержание)
___

Внести изменения в последний коммит можно параметром *commit* с флагом *--amend*, чтобы отредактировать сообщение предыдущего коммита, не изменяя его снимок:

    git commit --amend -m "New_name_commit"

Флаг *--no-edit* позволит внести в коммит поправку без изменения сообщения коммита. В этом случае итоговый коммит заменит неполный, а выглядеть это будет так, как будто мы отправили изменения ко всем файлам в нужных папках как единый снимок:

    git commit --amend --no-edit

Однако данный флаг подходит для работы с локальными коммитами, а не с публичными. Заменённые коммиты являются совершенно новыми, а предыдущий коммит уже не будет доступен в текущей ветке.

## [Создание новой ветки и переход в неё](#содержание)
___

Создать новую ветку можно с помощью параметра *branch*, указав имя ветки:
    
    git branch new_branch_name

Но **Git** не переключится на неё автоматически. Для автоматического перехода нужно добавить флаг *-b* и параметр *checkout*:

    git checkout -b new_branch_name

Либо возспользоваться 

    git checkout branch_name

## [Просмотр списка веток](#содержание)
___

Можно просматривать полный список веток, используя параметр *branch*. Команда отобразит все ветки, отметит текущую звёздочкой (*) и выделит её цветом.

    git branch

Также можно вывести список удалённых веток с помощью флага *-a*

    git branch -a

## [Удаление ветки](#содержание)
___

Удалить ветку можно параметром *branch* с добавлением флага *-d* и указанием имени ветки. Если вы завершили работу над веткой и объединили её с основной, можно её удалить без потери истории. Однако, если выполнить команду удаления до слияния — в результате появится сообщение об ошибке. Этот защитный механизм предотвращает потерю доступа к файлам.

    git branch -d 

Для принудительного удаления ветки используется флаг *-D* с заглавной буквой. В этом случае ветка будет удалена независимо от текущего статуса, без предупреждений.

    git branch -D existing_branch_name

Вышеуказанные команды удаляют только локальную копию ветки. В удалённом репозитории она может сохраниться. Если хотите стереть удалённую ветку, выполните следующую команду:

    git push origin --delete existing_branch_name

## [Слияние веток](#содержание)
___
Объединить две ветки можно параметром *merge* с указанием имени ветки. Команда объединит указанную ветку с основной.

    git merge existing_branch_name

## [Отображение журнала фиксации в виде графика для текущей или всех веток](#содержание)
___

Просмотреть историю коммитов в виде графика для текущей ветки можно с помощью параметра *log* и флагов *--graph --oneline --decorate*. Опция *--graph* выведет график в формате ASCII, отражающий структуру ветвления истории коммитов. В связке с флагами *--oneline* и *--decorate*, этот флаг упрощает понимание того, к какой ветке относится каждый коммит.

    git log --graph --oneline --decorate

## [Отправка изменений в удалённый репозиторий](#содержание)
___

Отправлять изменения в удалённый репозиторий можно параметром *push* с указанием имени репозитория и ветки.

    git push origin main

Эта команда передаёт локальные изменения в центральный репозиторий, где с ними могут ознакомиться другие участники проекта.

## [Получение изменений из удалённого репозитория](#содержание)
___

Для загрузки изменений из удалённого репозитория используется параметр *pull*. Он скачивает копию текущей ветки с указанного удалённого репозитория и объединяет её с локальной копией.

    git pull

## [Слияние удалённого репозитория с локальным](#содержание)
___

Слияние удалённого репозитория с локальным выполняется параметром *merge* с указанием имени удалённого репозитория.

    git merge origin

# [Заключение](#содержание)

Мы познакомились с основными тонкостями грамматики языка Markdown и командами **Git-а**.\
_**Если вам есть что добавить - всега готов дополнить данное руководство или внести изменения.**_\
Всем спасибо за внимание!